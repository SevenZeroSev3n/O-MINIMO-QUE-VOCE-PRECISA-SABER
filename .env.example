# ========================================
# CONFIGURAÇÕES DO SERVIDOR
# ========================================

# Porta do servidor (padrão: 3000)
PORT=3000

# ========================================
# SEGURANÇA
# ========================================

# Secret key para JWT (MUDE PARA UM VALOR FORTE E ÚNICO EM PRODUÇÃO!)
# Gere um valor aleatório com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=seu-secret-super-seguro-mude-isso-em-producao

# Secret key para registro de admin (deixe vazio para desabilitar registro)
# Gere um valor aleatório com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_REGISTRATION_SECRET=sua-chave-de-registro-admin-super-secreta

# ========================================
# CORS
# ========================================

# Origens permitidas para CORS (separadas por vírgula)
# Em desenvolvimento: http://localhost:5173
# Em produção: https://seu-dominio.com,https://www.seu-dominio.com
ALLOWED_ORIGINS=http://localhost:5173

# ========================================
# BANCO DE DADOS
# ========================================

# URL do banco de dados SQLite (usado pelo Prisma)
DATABASE_URL=file:./dev.db

# ========================================
# NOTAS DE SEGURANÇA
# ========================================

# 1. NUNCA comite o arquivo .env no git
# 2. Use valores fortes e únicos para JWT_SECRET em produção
# 3. Mantenha ADMIN_REGISTRATION_SECRET vazio após criar o primeiro admin
# 4. Em produção, use HTTPS e configure ALLOWED_ORIGINS corretamente
# 5. Considere usar um serviço de gerenciamento de secrets em produção (AWS Secrets Manager, etc.)

# ========================================
# INSTRUÇÕES PARA PRODUÇÃO
# ========================================

# 1. Copie este arquivo: cp .env.example .env
# 2. Gere secrets fortes usando os comandos acima
# 3. Configure as origens CORS para seu domínio de produção
# 4. Após criar o primeiro admin, remova ou deixe vazio ADMIN_REGISTRATION_SECRET
# 5. Certifique-se de que .env está no .gitignore
